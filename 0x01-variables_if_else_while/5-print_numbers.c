#include <stdio.h>
/**
  * main - Entry point
  *
  * Description: print single digigs in base 10
  *
  * Return: Always (0) success
  */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d\n", num);
	return (0);
}
