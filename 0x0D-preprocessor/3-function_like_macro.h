#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
  * file - function like macro
  * @x: number
  * Description: find absolute number of a number
  */
#define ABS(x) (((x) < 0) ? -(x) : (x))

#endif
