#include "main.h"
/**
  * _abs - absolute number
  *
  * Description: print absolute number of an integar
  *
  * @n: input integar
  *
  * Return: integar
  */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
